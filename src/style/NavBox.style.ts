import styled from "styled-components";

export const NavBox = styled.div`
  display: flex;
  flex-direction: row;
  padding: 20px 0;
  color: var(--darkGray);
  font-size: 15px;
  font-weight: 500;
`;
